// Temporary component to check working of materia-ui
import React from "react";
import Button from '@material-ui/core/Button';

export const MyCmp = () => {
    return (
        <Button variant="contained" color="primary">
            Hello World!
        </Button>
    )
}